// Solution returns true if the first argument passed in ends with the 2nd argument

function solution(str, ending){
    //Use endsWith to find the ending of the str, and compare it to the ending
    return str.endsWith(ending);
  }